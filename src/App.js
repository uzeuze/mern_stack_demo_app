var ResolutionList = React.createClass({
  render: function () {
    return (
      <div>
        Resolution List
      </div>
    );
  }
});

ReactDOM.render(
  <ResolutionList />,
  document.getElementById('main')
);
